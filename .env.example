# 复制这个文件为 server/.env 并填好。任何必填项缺失,服务都会启动失败。

# MySQL 连接(分开五项,比单一 DATABASE_URL 直观)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=qqnotice
DB_USER=root
DB_PASSWORD=cloud123

# Web 鉴权
JWT_SECRET=change-me-to-something-long
INVITE_CODE=change-me-too
ADMIN_USERNAME=admin

# 可选
PORT=3000
NODE_ENV=development
